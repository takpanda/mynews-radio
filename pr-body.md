## 変更内容

- 番組生成パネルに優先テーマ・除外テーマ・尺プリセットの設定UIを追加
- 設定APIのNext.jsプロキシとクライアントAPIを追加
- 保存中・保存失敗・初期化・未設定の状態を表示
- 生成完了時に今回適用した設定を表示

## 確認

- `npm test -- --runInBand`（143 tests passed）
- `npx tsc --noEmit`
- `npm run build`

## 補足

未設定時はバックエンド契約の既定値を利用し、既存の標準生成フローを維持します。
